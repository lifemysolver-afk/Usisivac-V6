# Post-Action Verification Protocol

1. **Verify Completion**: Run `python3 judge_guard.py "VERIFY_COMPLETION"`.
2. **Update Work Log**: Mark the action as done in `WORK_LOG.md`.
3. **Commit**: Execute `git commit -m "checkpoint: [ACTION]"` to save progress.
